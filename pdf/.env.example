# Example .env for pdf service
# Replace the value with your Google Generative API key (keep secret)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Alternative: if using a service account, point to the JSON key file
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json
